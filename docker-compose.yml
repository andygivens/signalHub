version: '3.8'
services:
  signalhub:
    build: .
    ports:
      - "2525:2525"
      - "8080:8080"
    env_file: .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./queue:/queue
    restart: unless-stopped
